{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --parallel",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "eslint": "7.32.0",
    "prettier": "^2.5.1",
    "turbo": "^1.9.3"
  }
}
