{
  "$schema": "https://json.schemastore.org/package.json",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --parallel",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "eslint": "8.47.0",
    "prettier": "^3.0.0",
    "turbo": "^1.10.12",
    "typescript": "^5.1.6"
  }
}
